# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
title: DnC Lab.
description: >
  ...
baseurl: ""
# url: "http://yourdomain.com"

timezone: Asia/Seoul

# Markdown settings
markdown: kramdown

# Sass settings
sass:
  add_charset: true
  sass_dir: assets/sass
  style: :compressed

# Build settings
exclude:
  - .vscode
  - .old.zip
  - _publications
  - README.md
  - package.json
  # - CNAME
  # - assets/sass

# Plugin settings
# plugins:
#   - jekyll-feed
#   - jekyll-sitemap

defaultCover: assets/images/members/default.webp